rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /sessions/{sessionId} {
      // Allow read/write only if the deviceId in the document matches the request
      allow read, write: if request.auth == null &&
        (resource == null || resource.data.deviceId is string);
    }
  }
}
